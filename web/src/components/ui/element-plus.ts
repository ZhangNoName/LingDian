/**
 * The single Element Plus boundary for merchant UI code.
 * Keep business pages importing primitives from here so the underlying
 * component library can be changed or restricted without touching each page.
 */
export {
  ElButton,
  ElCard,
  ElDatePicker,
  ElDialog,
  ElEmpty,
  ElForm,
  ElFormItem,
  ElInput,
  ElInputNumber,
  ElMessage,
  ElMessageBox,
  ElOption,
  ElPagination,
  ElRadio,
  ElSelect,
  ElSwitch,
  ElTable,
  ElTableColumn,
  ElTag,
  ElTimeline,
  ElTimelineItem,
  vLoading,
} from 'element-plus'
